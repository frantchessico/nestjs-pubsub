{
  "name": "@savanapoint/nestjs-pubsub",
  "version": "0.0.4",
  "description": "A library for integrating Firebase Pub/Sub with NestJS.",
  "author": "Francisco Inoque",
  "license": "MIT",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.lib.json",
    "prepublishOnly": "npm run build",
    "clean": "rimraf dist",
    "prepare": "npm run clean && npm run build"
  },
  "dependencies": {
    "@nestjs/common": "^9.0.0",
    "@nestjs/core": "^9.0.0",
    "firebase": "^9.0.0",
    "rxjs": "^7.0.0"
  },
  "devDependencies": {
    "@nestjs/cli": "^9.0.0",
    "typescript": "^5.5.4",
    "rimraf": "^5.0.0",
    "ts-jest": "^28.0.0",
    "jest": "^28.0.0",
    "@types/jest": "^28.0.0"
  },
  "files": [
    "dist/**/*"
  ],
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/frantchessico/nestjs-pubsub.git"
  },
  "keywords": [
    "nestjs",
    "firebase",
    "pubsub",
    "library"
  ]
}
